#ifndef WORLD_COUNTER_H
#define WORLD_COUNTER_H

#include <iostream>

size_t word_count_slow(const char* const str, const size_t size);
size_t word_count_fast(const char* const str, const size_t size);

#endif